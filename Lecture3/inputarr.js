const finalPraticcipants = ["taylor", "donal", "don", "natasha", "bobby"];

const announment = finalPraticcipants.map((member) => {
    return member + " joined the contest";
});

console.log(announment);
